#include <stdio.h>
void foo(int xval)
{
	int x;
	x = xval;
	/* Τυπώστε τη διεύθυνση και την τιμή της x εδώ. */
}

void bar(int yval)
{
	int y;
	/* Τυπώστε τη διεύθυνση και την τιμή της y εδώ. */
}

int main()
{
	foo(5);
	bar(3);
	return 0;
}
